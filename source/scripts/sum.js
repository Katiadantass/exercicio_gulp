// Função que realiza a soma de dois números
function somar(a, b) {
  return a + b;  // Retorna a soma dos dois números
}

// Exporta a função somar para ser usada em outros arquivos
module.exports = somar;